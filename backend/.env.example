# Local MySQL configuration for the backend (copy to .env)
#
# cp .env.example .env
#
# IMPORTANT:
# - .env is ignored by git (see backend/.gitignore)
# - Only use this for local development

DB_HOST=localhost
DB_PORT=3306
DB_NAME=fedfdb
DB_USER=root
DB_PASSWORD=YOUR_MYSQL_PASSWORD
